<div>
  <div>
    <div>
        <h2>
        <%= @post.title %> by <%= @user.name %>
        </h2>
      <div>
        <p>Comments : <%= @post.comments.size%>  Likes : <%= @post.likes.size %> </p>
      </div>
    </div>
    <p><%= @post.text %> </p>
  </div>
    <%= render partial: "comments_section", locals: { comments: @post.comments } %>
    <%= render partial: "comments/new_comment" %>
</div>